alias: Huawei Discharging Power 0W
description: >-
  Sets the battery's maximum discharging power to 0W if it's currently not zero.
  This helps to prevent power discharge when not needed.
icon: mdi:battery-off
mode: single
sequence:
  - condition: template
    value_template: "{{ states(\"number.battery_maximum_discharging_power\") | int(0) != 0 }}"
  - action: number.set_value
    target:
      entity_id: number.battery_maximum_discharging_power
    data:
      value: "0"
